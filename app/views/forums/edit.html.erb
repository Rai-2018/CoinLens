<%= form_for @forum do |f| %>
	<%= f.label :name, "Forum Name" %>
	<%= f.text_field :name %>
	<%= f.label :description, "Description" %>
	<%= f.text_area :description %>
	<%= f.submit "Save Changes" %>
<% end %>